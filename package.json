{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "build-json": "yarn workspace gpx-to-json build $INIT_CWD/gpx-input",
    "build-ui": "yarn workspace ui build",
    "watch-ui": "yarn workspace ui watch"
  },
  "license": "ISC",
  "packageManager": "yarn@3.1.1"
}
